{
  "name": "lacerba_db",
  "version": "1.0.0",
  "description": "first_database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "npx prisma migrate dev && npx prisma generate",
    "migrate:make": "npx prisma migrate dev --create-only"
  },
  "author": "ap999nea",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^3.14.0",
    "express": "^4.18.1",
    "typescript": "^4.6.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.35",
    "prisma": "^3.14.0"
  }
}
